/* pocketmod is plain C (it relies on implicit void* conversions), so its
 * implementation lives in this C translation unit; the C++ sources just
 * include the header for the API. */
#define POCKETMOD_IMPLEMENTATION
#include "pocketmod.h"
